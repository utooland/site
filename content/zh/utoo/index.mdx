# utoo

import { Callout, Cards, Tabs } from 'nextra/components'

utoo (`ut`) 是一个快速、现代的 JavaScript 包管理器，提供与 npm 兼容的包管理功能，并具有性能优化。

<Callout type="info">
  utoo 是 npm 的直接替代品，性能更优。
</Callout>

## 安装

<Tabs items={['macOS / Linux', 'Windows', 'npm']}>
  <Tabs.Tab>
    无需 Node.js，一行命令安装：

    ```bash filename="Terminal" copy
    curl -fsSL https://utoo.land/install | bash
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    在 PowerShell 中运行：

    ```powershell filename="PowerShell" copy
    irm https://utoo.land/install.ps1 | iex
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    如果你已经安装了 Node.js：

    ```bash filename="Terminal" copy
    npm install -g utoo
    ```
  </Tabs.Tab>
</Tabs>

## 快速开始

### 1. 安装依赖

```bash filename="Terminal" copy
ut install
```

### 2. 添加包

```bash filename="Terminal" copy
ut install lodash
ut install -D typescript
```

### 3. 运行脚本

```bash filename="Terminal" copy
ut build
ut dev
```

### 4. 执行远程包

```bash filename="Terminal" copy
utx create-react-app my-app
```

## 性能对比

`utoo` 的性能优势主要体现在依赖解析、下载复用和全局包缓存复用。以下是 2026-08-27 在 Linux China x86_64 环境直连 `https://registry.npmmirror.com` 得到的 `ant-design` 阶段化安装基准，耗时越低越好：

| 场景 | utoo | Bun | pnpm 10 | pnpm 12 | aube |
|------|------|-----|---------|---------|------|
| 完全冷启动 (`p0`) | **12.04s ± 1.25s** | 19.63s ± 1.32s | 33.88s ± 0.64s | 18.50s ± 6.22s | 26.47s ± 7.65s |
| 仅 lock 冷启动 (`p3`) | **22.28s ± 12.71s** | 31.75s ± 15.11s | 40.70s ± 11.64s | 31.67s ± 10.09s | — |
| 热缓存安装 (`p4`) | **1.22s ± 0.07s** | 4.58s ± 0.32s | 9.86s ± 0.48s | 2.60s ± 0.09s | — |

utoo 在三个有效安装场景中的均值都最快；`p0` / `p3` 波动较大，应视为方向性结果，低方差的 `p4` 信号最稳定。aube 的 `p3` / `p4` 因缺少预期 lockfile 被拒绝。GitHub Actions npmjs 全量结果见[性能基准](/zh/docs/utoo/performance)。

## 命令

<Cards>
  <Cards.Card title="install" href="/zh/docs/utoo/commands#install" />
  <Cards.Card title="run" href="/zh/docs/utoo/commands#run" />
  <Cards.Card title="execute" href="/zh/docs/utoo/commands#execute" />
  <Cards.Card title="view" href="/zh/docs/utoo/commands#view" />
  <Cards.Card title="性能基准" href="/zh/docs/utoo/performance" />
</Cards>

**所有命令**

| 命令 | 别名 | 描述 |
|------|------|------|
| [`install`](/zh/docs/utoo/commands#install) | `i` | 安装项目依赖 |
| [`uninstall`](/zh/docs/utoo/commands#uninstall) | `un` | 卸载指定依赖 |
| [`run`](/zh/docs/utoo/commands#run) | `r` | 运行 package.json 中定义的脚本 |
| [`execute`](/zh/docs/utoo/commands#execute) | `x` | 执行来自本地或远程 npm 包的命令 |
| [`view`](/zh/docs/utoo/commands#view) | `v`, `info`, `show` | 查看包信息 |
| [`list`](/zh/docs/utoo/commands#list) | `ls` | 列出依赖（类似 npm list） |
| [`link`](/zh/docs/utoo/commands#link) | `ln` | 链接包（类似 npm link） |
| [`deps`](/zh/docs/utoo/commands#deps) | `d` | 仅生成 package-lock.json |
| [`update`](/zh/docs/utoo/commands#update) | `u` | 删除 node_modules 并重新安装 |
| [`rebuild`](/zh/docs/utoo/commands#rebuild) | `rb` | 重跑所有包的脚本钩子 |
| [`clean`](/zh/docs/utoo/commands#clean) | `c` | 清理全局存储中的包缓存 |
| [`pm-pack`](/zh/docs/utoo/commands#pm-pack) | `pk` | 创建包的 tarball |
| [`ping`](/zh/docs/utoo/commands#ping) | `pg` | 检测镜像源连通性 |
| [`login`](/zh/docs/utoo/commands#login) | `lg` | 登录镜像源 |
| [`logout`](/zh/docs/utoo/commands#logout) | `lo` | 注销登录 |
| [`whoami`](/zh/docs/utoo/commands#whoami) | `who` | 显示当前用户名 |
| [`config`](/zh/docs/utoo/config) | `cfg` | 管理配置 |
| [`init`](/zh/docs/utoo/commands#init) | `create` | 创建 package.json 文件 |
| [`publish`](/zh/docs/utoo/commands#publish) | `pub` | 发布包到镜像源 |
| [`completions`](/zh/docs/utoo/commands#completions) | `cmp` | 生成 shell 自动补全脚本 |
| `*` | — | 任何未识别命令会走到 `ut run *` |

每个命令自身的选项列在对应章节里；也可以直接 `ut <command> --help` 在终端查看。

## 全局选项

| 选项 | 描述 |
|------|------|
| `-h, --help` | 显示帮助 |
| `-v, --version` | 显示版本 |

## 特性

### 全局缓存

安装过的包会按 `包名/版本` 缓存到 `~/.cache/nm/`，后续安装直接从缓存硬链接到项目，跳过下载和解压。同一版本的包在磁盘上只存储一份，跨项目共享。

```
~/.cache/nm/
├── lodash/4.17.21.tgz
├── react/18.3.0.tgz
└── typescript/5.6.0.tgz
```

### 镜像源加速

当使用的 registry 是 cnpm 兼容镜像（如 `registry.npmmirror.com`）时，utoo 自动启用两项加速：**semver 查询加速**（服务端解析版本）和 **binary mirror**（原生模块下载改走 CDN）。两项在 npm 官方源上都会跳过。

具体机制见 [镜像源加速](/zh/docs/utoo/registry-acceleration)，命令行探测见 [`ut ping`](/zh/docs/utoo/commands#ping)。

### 从 pnpm 迁移

一行命令从 pnpm 项目迁移，自动转换 `pnpm-workspace.yaml` 中的工作区、overrides 和 catalog 配置：

```bash filename="Terminal" copy
ut install --from pnpm
```

详见[从 pnpm 迁移](/zh/docs/utoo/migration)。

## 快捷执行

<Callout>
  你可以直接运行 package.json 脚本，无需 `run`：
</Callout>

```bash filename="Terminal" copy
ut build      # 等同于: ut run build
ut dev        # 等同于: ut run dev
ut test       # 等同于: ut run test
```
