# list

列出已安装的依赖。

**别名:** `ls`

## 用法

```bash filename="Terminal"
ut list <PACKAGE>
```

## 参数

| 参数 | 描述 |
|------|------|
| `<PACKAGE>` | 要查看依赖的包名（必需） |

## 示例

```bash filename="Terminal" copy
# 列出指定包及其依赖
ut list react
ut ls lodash
```
