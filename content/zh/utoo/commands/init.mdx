# init

创建 package.json 文件。

**别名：** `create`

## 用法

```bash filename="Terminal"
ut init [OPTIONS]
```

## 示例

```bash filename="Terminal" copy
# 交互模式（提示输入名称、版本等）
ut init

# 跳过提示，使用默认值
ut init --yes
ut init -y
```

## 选项

| 选项 | 描述 |
|------|------|
| `-y, --yes` | 跳过提示，使用默认值 |
